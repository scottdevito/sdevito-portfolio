// Height of the app header bar
export const appHeaderHeight = `120px`;
// The width of the app's center content section
export const centerContentWidth = `65%`;
// The width of the nav links section
export const navLinksWidth = `40%`;
// The speed that auto scrolling will be done across the app
export const scrollSpeed = 1000;
